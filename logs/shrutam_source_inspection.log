FILE:model_meta/Shrutam-2/config.json
{
  "model_type": "shrutam2_asr",
  "architectures": ["ShrutamASR"],
  "languages": ["hi", "mr", "ta", "te", "ml", "kn", "or", "bn", "ur", "as", "gu", "pa"]
}
FILE:model_meta/Shrutam-2/inference_config.yaml
ckpt_path: "model.pt"

inference:
  freeze_encoder: true
  freeze_llm: true
  enable_fsdp: false
  enable_ddp: false
  model_name: "asr"

model:
  LLM_PATH: "llm"
  LLM_NAME: "llm"
  encoder_dim: 1024
  llm_dim: 2048
  encoder_projector_ds_rate: 1
  encoder_projector: "ds-conv-linear"
  encoder_name: "speech_encoder"
  moe_gating: true
  moe_routing: "smear_utter"
  num_experts: 8
  prompt_template: "<|im_start|>user\n{}<|im_end|>\n<|im_start|>assistant\n"
  tied_experts: false
  encoder_ds_rate: 2
FILE:model_meta/Shrutam-2/requirements.txt
transformers==4.56.2
huggingface_hub==0.36.0
pyyaml

cffi==2.0.0
soundfile==0.13.1
sympy==1.14.0
FILE:model_meta/Shrutam-2/inference_script.py
from huggingface_hub import hf_hub_download, snapshot_download
from speech_encoder import SpeechEncoder
import torch, torchaudio
import os
from asr_model import ASRModel, EncoderProjectorLinear, EncoderDownsamplerCov1d
from transformers import AutoTokenizer, AutoModelForCausalLM
from inference_config import inference_config, model_config

REPO_ID = "bharatgenai/Shrutam-2"

ckpt_path = hf_hub_download(repo_id=REPO_ID, filename="model.pt")
encoder_path = hf_hub_download(repo_id=REPO_ID, filename="encoder.pt")
llm_dir = snapshot_download(repo_id=REPO_ID, allow_patterns="llm/*")
llm_path = os.path.join(llm_dir, "llm")

encoder = SpeechEncoder(encoder_path)

def model_factory():
    tokenizer = AutoTokenizer.from_pretrained(llm_path)
    tokenizer.pad_token_id = tokenizer.eos_token_id

    llm = AutoModelForCausalLM.from_pretrained(
                llm_path,
                load_in_8bit=None,
                device_map=None,
                use_cache=None,
                trust_remote_code=True
            )
    for name, param in llm.named_parameters():
            param.requires_grad = False
    llm.eval()

    encoder_projector = [
        EncoderProjectorLinear(model_config) for _ in range(model_config.num_experts)
    ]
    down_sampler = EncoderDownsamplerCov1d(model_config)

    model = ASRModel(
        encoder=encoder,
        llm=llm,
        encoder_projector=encoder_projector,
        down_sampler=down_sampler,
        tokenizer=tokenizer,
        train_config=inference_config,
        model_config=model_config,
    )
    ckpt_dict = torch.load(ckpt_path, map_location="cpu")
    model.load_state_dict(ckpt_dict, strict=False)

    
    return model, tokenizer



model, tokenizer = model_factory()

model.to("cuda")
model.eval()

def inference(wav_path, prompt):
    wav, sr = torchaudio.load(wav_path)
    wav = wav.to("cuda")
    f, outlength = encoder.encode(wav)
    audio_pseudo = torch.full((outlength,), -1)
    audio_rep = f
    audio_rep = audio_rep.to("cuda")

    prompt = model_config.prompt_template.format(prompt)
    prompt_ids = tokenizer.encode(prompt)
    prompt_length = len(prompt_ids)

    prompt_ids = torch.tensor(prompt_ids, dtype=torch.int64)
    input_ids = torch.cat((audio_pseudo, prompt_ids)).unsqueeze(0).to("cuda")
    attention_mask = input_ids.ge(-1)
    attention_mask = attention_mask.to("cuda")

    modality_mask = torch.zeros_like(attention_mask)
    modality_mask[:, :outlength] = True
    modality_mask = modality_mask.to("cuda")

    model_outputs = model.generate(input_ids=input_ids, attention_mask=attention_mask, modality_mask=modality_mask, audio=wav)

    output_text = tokenizer.batch_decode(model_outputs, skip_special_tokens=True)

    return output_text

print(inference("blindtest_250138.wav", "Transcribe speech to Hindi text."))
FILE:model_meta/Shrutam-2/asr_model.py
import torch.nn as nn
from typing import Optional
from smear import MoELayer_SMEAR
import torch
from typing import List
from utils import compute_accuracy

class ASRModel(nn.Module):
    def __init__(self,
        encoder: nn.Module,
        llm: nn.Module,
        encoder_projector: list[nn.Module],
        tokenizer, 
        train_config, 
        model_config, 
        down_sampler: Optional[nn.Module] = None,
        **kwargs):

        super().__init__()

        self.encoder = encoder
        self.llm = llm
        self.encoder_projector = nn.ModuleList(encoder_projector)
        self.down_sampler = down_sampler

        self.MoELayer_routing = MoELayer_SMEAR(self.encoder_projector,
                        input_dim=model_config.encoder_dim)
        
        self.tokenizer = tokenizer
        self.train_config = train_config
        self.model_config = model_config
    
    def forward(self,
                input_ids: torch.LongTensor = None,
                attention_mask: Optional[torch.Tensor] = None,
                position_ids: Optional[torch.LongTensor] = None,
                past_key_values: Optional[List[torch.FloatTensor]] = None,
                inputs_embeds: Optional[torch.FloatTensor] = None,
                labels: Optional[torch.LongTensor] = None,
                use_cache: Optional[bool] = None,
                output_attentions: Optional[bool] = None,
                output_hidden_states: Optional[bool] = None,
                return_dict: Optional[bool] = None,
                **kwargs):

        audio = kwargs.get("audio", None)
        modality_mask = kwargs.get("modality_mask", None)
        audio_rep = kwargs.get("audio_rep", None)
        if audio_rep is None:
            audio_rep, audio_rep_length = self.encoder.encode(audio)
        audio_rep = audio_rep.to(self.down_sampler.conv1.weight.device)
        ds_encoder_outs = self.down_sampler(audio_rep)
        moe_out = self.MoELayer_routing(ds_encoder_outs)
        # print("MoE out shape: ", moe_out[0].shape)
        if len(moe_out) == 2:
            encoder_outs, load_balance_loss = moe_out
        elif len(moe_out) == 3:
            encoder_outs, load_balance_loss, router_info = moe_out
        
        if input_ids is not None:
            input_ids[input_ids == -1] = 0
            if hasattr(self.llm.model, "embed_tokens"):
                inputs_embeds = self.llm.model.embed_tokens(input_ids)
            elif hasattr(self.llm.model, "word_embeddings"):
                inputs_embeds = self.llm.model.word_embeddings(input_ids)
            else:
                inputs_embeds = self.llm.model.embed_tokens(input_ids)

        if modality_mask is not None:
            modality_mask_start_indices = (modality_mask == True).float().argmax(dim=1) # returns the starting index from where audio is begining
            modality_lengths = torch.clamp(modality_mask.sum(dim=1), max=encoder_outs.shape[1]).tolist()

            encoder_outs_pad = torch.zeros_like(inputs_embeds)
            for i in range(encoder_outs.shape[0]):
                encoder_outs_pad[
                    i, modality_mask_start_indices[i]:modality_mask_start_indices[i]+modality_lengths[i]
                ] = encoder_outs[i][:modality_lengths[i]]
            
            inputs_embeds = encoder_outs_pad + inputs_embeds * (~modality_mask[:, :, None])
        
        if kwargs.get("inference_mode", False):
            return inputs_embeds, attention_mask
        
        model_outputs = self.llm(inputs_embeds=inputs_embeds, attention_mask=attention_mask, labels=labels)
        acc = -1
        if self.metric:
            with torch.no_grad():
                preds = torch.argmax(model_outputs.logits, -1)
                acc = compute_accuracy(preds.detach()[:, :-1], labels.detach()[:, 1:], ignore_label=-100)
        
        if self.MoELayer_routing is not None:
            # print(f"load_balance_loss: {load_balance_loss}")
            if "router_info" in locals():
                return model_outputs, acc, load_balance_loss, router_info
            else:
                return model_outputs, acc, load_balance_loss
        else:
            return model_outputs, acc
    
    @torch.no_grad()
    def generate(self,
                input_ids: torch.LongTensor = None,
                attention_mask: Optional[torch.Tensor] = None,
                position_ids: Optional[torch.LongTensor] = None,
                past_key_values: Optional[List[torch.FloatTensor]] = None,
                inputs_embeds: Optional[torch.FloatTensor] = None,
                labels: Optional[torch.LongTensor] = None,  
                use_cache: Optional[bool] = None,
                output_attentions: Optional[bool] = None,
                output_hidden_states: Optional[bool] = None,
                return_dict: Optional[bool] = None,
                **kwargs,
                ):
        kwargs["inference_mode"] = True


        inputs_embeds, attention_mask = self.forward(
            input_ids=input_ids,
            attention_mask=attention_mask,
            position_ids=position_ids,
            past_key_values=past_key_values,
            inputs_embeds=inputs_embeds,
            labels=labels,
            use_cache=use_cache,
            output_attentions=output_attentions,
            output_hidden_states=output_hidden_states,
            return_dict=return_dict,
            **kwargs,
        )

        model_outputs = self.llm.generate(
            inputs_embeds=inputs_embeds,
            max_length=kwargs.get("max_length", 200),
            max_new_tokens=kwargs.get("max_new_tokens", 200),
            num_beams=kwargs.get("num_beams", 4),
            # num_beam_groups=kwargs.get("num_beams", 4),
            num_beam_groups=1,
            do_sample=kwargs.get("do_sample", False),
            min_length=kwargs.get("min_length", 1),
            top_p=kwargs.get("top_p", 1.0),
            repetition_penalty=kwargs.get("repetition_penalty", 1.3),
            length_penalty=kwargs.get("length_penalty", 0.8),
            temperature=kwargs.get("temperature", 1.0),
            attention_mask=attention_mask,
            # no_repeat_ngram_size=3,
            # diversity_penalty=0.3,
            bos_token_id=self.tokenizer.bos_token_id,
            eos_token_id=self.tokenizer.eos_token_id,
            pad_token_id=self.tokenizer.pad_token_id
        )
        
        return model_outputs

class EncoderProjectorLinear(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.encoder_dim = config.encoder_dim
        self.llm_dim = config.llm_dim
        self.relu1 = nn.ReLU()
        self.linear1 = nn.Linear(self.encoder_dim, 2048)
        self.relu2 = nn.ReLU()
        self.linear2 = nn.Linear(2048, self.llm_dim)
    
    def forward(self, x):
        x = self.relu1(x)
        x = self.linear1(x)
        x = self.relu2(x)
        x = self.linear2(x)
        return x

class EncoderDownsamplerCov1d(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.k = config.encoder_projector_ds_rate
        self.encoder_dim = config.encoder_dim
        self.llm_dim = config.llm_dim
        self.conv1 = nn.Conv1d(
            in_channels=self.encoder_dim,
            out_channels=self.encoder_dim,
            kernel_size=3,
            stride=1,
            padding=1
        )
        self.relu = nn.ReLU()
        self.conv2 = nn.Conv1d(
            in_channels=self.encoder_dim,
            out_channels=self.encoder_dim,
            kernel_size=self.k,
            stride=self.k,
            padding=0
        )
    
    def forward(self, x):
        x = x.transpose(1, 2)
        x = self.conv1(x)
        x = self.relu(x)
        x = self.conv2(x)
        x = x.transpose(1, 2)
        return xFILE:model_meta/Shrutam-2/speech_encoder.py
import math
import os
from typing import Union

import torch
import torch.nn as nn
import torch.nn.functional as F
import torchaudio

from conformer_encoder import ConformerEncoder

DEFAULT_CHECKPOINT = os.path.join(os.path.dirname(os.path.abspath(__file__)), "encoder.pt")


class MelSpectrogramPreprocessor(nn.Module):
    """Python log-mel spectrogram feature extractor."""

    def __init__(
        self,
        sample_rate: int = 16000,
        n_fft: int = 512,
        win_length: int = 400,
        hop_length: int = 160,
        n_mels: int = 80,
        preemph: float = 0.97,
        dither: float = 0.0,
        normalize: str = "per_feature",
        log_zero_guard: float = 2**-24,
        exact_pad: bool = False,
    ):
        super().__init__()
        self.preemph = preemph
        self.dither = dither
        self.normalize_type = normalize
        self.log_zero_guard = log_zero_guard
        self.exact_pad = exact_pad
        self.hop_length = hop_length
        self.n_fft = n_fft

        self.register_buffer("window", torch.hann_window(win_length))

        n_freqs = n_fft // 2 + 1
        f_max = sample_rate / 2.0
        mel_low = 2595.0 * math.log10(1.0 + 0.0 / 700.0)
        mel_high = 2595.0 * math.log10(1.0 + f_max / 700.0)
        mel_points = torch.linspace(mel_low, mel_high, n_mels + 2)
        hz_points = 700.0 * (10.0 ** (mel_points / 2595.0) - 1.0)
        freq_bins = torch.linspace(0.0, f_max, n_freqs)

        fb = torch.zeros(n_freqs, n_mels)
        for i in range(n_mels):
            lo, mid, hi = hz_points[i], hz_points[i + 1], hz_points[i + 2]
            fb[:, i] = torch.clamp(
                torch.minimum(
                    (freq_bins - lo) / (mid - lo + 1e-10),
                    (hi - freq_bins) / (hi - mid + 1e-10),
                ),
                min=0.0,
            )
        self.register_buffer("fb", fb)

    def forward(self, input_signal: torch.Tensor, length: torch.Tensor):
        x = input_signal

        if self.training and self.dither > 0.0:
            x = x + self.dither * torch.randn_like(x)

        if self.preemph and self.preemph > 0.0:
            x = torch.cat([x[:, :1], x[:, 1:] - self.preemph * x[:, :-1]], dim=1)

        if self.exact_pad:
            x = F.pad(x, (self.n_fft // 2, self.n_fft // 2), mode="reflect")

        spec = torch.stft(
            x,
            n_fft=self.n_fft,
            hop_length=self.hop_length,
            win_length=self.window.size(0),
            window=self.window,
            center=not self.exact_pad,
            return_complex=True,
        )

        power = spec.abs().pow(2)
        mel = torch.matmul(power.transpose(1, 2), self.fb).transpose(1, 2)
        log_mel = torch.log(mel + self.log_zero_guard)

        out_length = torch.div(length, self.hop_length, rounding_mode="floor") + 1

        if self.normalize_type == "per_feature":
            mean = log_mel.mean(dim=-1, keepdim=True)
            std = log_mel.std(dim=-1, keepdim=True)
            log_mel = (log_mel - mean) / (std + 1e-5)

        return log_mel, out_length


class SpeechEncoder:

    def __init__(self, checkpoint_path: str = DEFAULT_CHECKPOINT):
        self.device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
        self.preprocessor = MelSpectrogramPreprocessor().to(self.device)
        self.preprocessor.eval()
        self.model = ConformerEncoder().to(self.device)
        state_dict = torch.load(checkpoint_path, map_location=self.device, weights_only=True)
        self.model.load_state_dict(state_dict)
        self.model.eval()

    @torch.no_grad()
    def _preprocess(self, wav: torch.Tensor, lengths: torch.Tensor):
        return self.preprocessor(
            input_signal=wav.to(self.device),
            length=lengths.to(self.device),
        )

    @torch.no_grad()
    def _extract_features(self, wav: torch.Tensor):
        audio_signal, length = self._preprocess(wav, torch.tensor([wav.shape[-1]]))
        features, out_lengths = self.model(audio_signal, length)
        return features.permute(0, 2, 1), out_lengths

    @torch.no_grad()
    def _extract_features_batched(self, wav: torch.Tensor, lengths: torch.Tensor):
        audio_signal, length = self._preprocess(wav, lengths)
        features, out_lengths = self.model(audio_signal, length)
        return features.permute(0, 2, 1), out_lengths

    def _pad(self, wav, target_length):
        current_length = wav.shape[1]
        if current_length >= target_length:
            return wav
        padding = torch.zeros(1, target_length - current_length)
        return torch.cat([wav, padding], dim=1)

    def encode(self, wav: Union[list, torch.Tensor]):
        if isinstance(wav, list):
            max_wav_len = max(w.shape[1] for w in wav)
            padded_wavs = torch.stack([self._pad(w, max_wav_len) for w in wav]).to(
                self.device, non_blocking=True
            )
            lengths = torch.tensor([w.shape[1] for w in wav]).to(
                self.device, non_blocking=True
            )
            padded_wavs = padded_wavs.squeeze(1)
            features, out_lengths = self._extract_features_batched(padded_wavs, lengths)
        elif isinstance(wav, torch.Tensor):
            features, out_lengths = self._extract_features(wav)
        else:
            raise ValueError("Input must be a list of tensors or a single tensor.")
        return features, out_lengths



FILE:model_meta/Shrutam-2/conformer_encoder.py
import math
import torch
import torch.nn as nn
import torch.nn.functional as F


class ConvSubsampling(nn.Module):
    """Convolutional subsampling with depthwise-separable convolutions.
    
    Reduces time by 8x and maps mel features to d_model.
    Input: [B, n_mel, T], Output: [B, T//8, d_model]
    """

    def __init__(self, n_mel=80, d_model=1024, channels=256):
        super().__init__()
        self.conv = nn.Sequential(
            nn.Conv2d(1, channels, 3, stride=2, padding=1),
            nn.ReLU(),
            nn.Conv2d(channels, channels, 3, stride=2, padding=1, groups=channels),
            nn.Conv2d(channels, channels, 1),
            nn.ReLU(),
            nn.Conv2d(channels, channels, 3, stride=2, padding=1, groups=channels),
            nn.Conv2d(channels, channels, 1),
            nn.ReLU(),
        )
        self.out = nn.Linear(channels * (n_mel // 8), d_model)

    def forward(self, x, lengths):
        # x: [B, n_mel, T] -> [B, 1, T, n_mel]
        x = x.transpose(1, 2).unsqueeze(1)
        x = self.conv(x)                               # [B, C, T//8, F//8]
        B, C, T, F = x.shape
        x = x.permute(0, 2, 1, 3).contiguous().view(B, T, C * F)
        x = self.out(x)                                # [B, T//8, d_model]
        for _ in range(3):
            lengths = (lengths + 1) // 2
        return x, lengths


class RelativePositionalEncoding(nn.Module):
    """Sinusoidal relative positional encoding."""

    def __init__(self, d_model=1024, max_len=5000):
        super().__init__()
        self.max_len = max_len
        pe = torch.zeros(2 * max_len - 1, d_model)
        positions = torch.arange(0, 2 * max_len - 1, dtype=torch.float32).unsqueeze(1)
        div_term = torch.exp(
            torch.arange(0, d_model, 2, dtype=torch.float32) * -(math.log(10000.0) / d_model)
        )
        pe[:, 0::2] = torch.sin(positions * div_term)
        pe[:, 1::2] = torch.cos(positions * div_term)
        self.register_buffer("pe", pe.unsqueeze(0))     # [1, 2*max_len-1, d_model]

    def forward(self, seq_len):
        start = self.max_len - seq_len
        end = self.max_len + seq_len - 1
        return self.pe[:, start:end]                    # [1, 2*seq_len-1, d_model]


class MultiHeadRelPositionAttention(nn.Module):
    """Multi-head attention with relative positional encoding (Transformer-XL style)."""

    def __init__(self, d_model=1024, n_heads=8, dropout=0.1):
        super().__init__()
        self.d_model = d_model
        self.n_heads = n_heads
        self.d_head = d_model // n_heads

        self.linear_q = nn.Linear(d_model, d_model)
        self.linear_k = nn.Linear(d_model, d_model)
        self.linear_v = nn.Linear(d_model, d_model)
        self.linear_pos = nn.Linear(d_model, d_model, bias=False)
        self.linear_out = nn.Linear(d_model, d_model)

        self.pos_bias_u = nn.Parameter(torch.zeros(n_heads, self.d_head))
        self.pos_bias_v = nn.Parameter(torch.zeros(n_heads, self.d_head))

        self.dropout = nn.Dropout(dropout)
        self.scale = 1.0 / math.sqrt(self.d_head)

    def _rel_shift(self, x):
        """Relative position shift to align position scores with content scores."""
        B, H, T, P = x.size()
        x = F.pad(x, (1, 0))                           # [B, H, T, P+1]
        x = x.view(B, H, P + 1, T)                     # [B, H, P+1, T]
        x = x[:, :, 1:, :].view(B, H, T, P)            # [B, H, T, P]
        return x[:, :, :, :T]                           # [B, H, T, T]

    def forward(self, x, pos_emb, mask=None):
        B, T, _ = x.shape

        q = self.linear_q(x).view(B, T, self.n_heads, self.d_head).transpose(1, 2)
        k = self.linear_k(x).view(B, T, self.n_heads, self.d_head).transpose(1, 2)
        v = self.linear_v(x).view(B, T, self.n_heads, self.d_head).transpose(1, 2)

        p = self.linear_pos(pos_emb).view(1, -1, self.n_heads, self.d_head).transpose(1, 2)

        # Content attention: (q + bias_u) @ k^T
        q_with_u = q + self.pos_bias_u.unsqueeze(0).unsqueeze(2)
        content_score = torch.matmul(q_with_u, k.transpose(-2, -1))

        # Position attention: (q + bias_v) @ p^T, then relative shift
        q_with_v = q + self.pos_bias_v.unsqueeze(0).unsqueeze(2)
        pos_score = torch.matmul(q_with_v, p.transpose(-2, -1))
        pos_score = self._rel_shift(pos_score)

        scores = (content_score + pos_score) * self.scale

        if mask is not None:
            scores = scores.masked_fill(~mask.unsqueeze(1).unsqueeze(2), float("-inf"))

        attn_weights = self.dropout(torch.softmax(scores, dim=-1))
        attn_out = torch.matmul(attn_weights, v)

        attn_out = attn_out.transpose(1, 2).contiguous().view(B, T, self.d_model)
        return self.linear_out(attn_out)


class FeedForwardModule(nn.Module):
    """Position-wise feed-forward with SiLU activation."""

    def __init__(self, d_model=1024, ff_dim=4096, dropout=0.1):
        super().__init__()
        self.linear1 = nn.Linear(d_model, ff_dim)
        self.linear2 = nn.Linear(ff_dim, d_model)
        self.dropout = nn.Dropout(dropout)

    def forward(self, x):
        return self.dropout(self.linear2(self.dropout(F.silu(self.linear1(x)))))


class ConvolutionModule(nn.Module):
    """Conformer convolution module: pointwise -> GLU -> depthwise -> act -> pointwise."""

    def __init__(self, d_model=1024, kernel_size=9, dropout=0.1):
        super().__init__()
        self.pointwise_conv1 = nn.Conv1d(d_model, 2 * d_model, 1)
        self.depthwise_conv = nn.Conv1d(
            d_model, d_model, kernel_size,
            padding=(kernel_size - 1) // 2, groups=d_model
        )
        self.pointwise_conv2 = nn.Conv1d(d_model, d_model, 1)
        self.dropout = nn.Dropout(dropout)

    def forward(self, x, mask=None):
        x = x.transpose(1, 2)
        x = self.pointwise_conv1(x)                    # [B, 2D, T]
        x = F.glu(x, dim=1)                            # [B, D, T]
        if mask is not None:
            x = x * mask.unsqueeze(1).float()
        x = self.depthwise_conv(x)                     # [B, D, T]
        x = F.silu(x)
        x = self.pointwise_conv2(x)                    # [B, D, T]
        return self.dropout(x.transpose(1, 2))          # [B, T, D]


class ConformerBlock(nn.Module):
    """Single Conformer block: FF -> Attn -> Conv -> FF (Macaron-style)."""

    def __init__(self, d_model=1024, n_heads=8, ff_dim=4096, conv_kernel_size=9, dropout=0.1):
        super().__init__()
        self.norm_ff1 = nn.LayerNorm(d_model)
        self.ff1 = FeedForwardModule(d_model, ff_dim, dropout)
        self.norm_attn = nn.LayerNorm(d_model)
        self.attn = MultiHeadRelPositionAttention(d_model, n_heads, dropout)
        self.norm_conv = nn.LayerNorm(d_model)
        self.conv = ConvolutionModule(d_model, conv_kernel_size, dropout)
        self.norm_ff2 = nn.LayerNorm(d_model)
        self.ff2 = FeedForwardModule(d_model, ff_dim, dropout)
        self.norm_out = nn.LayerNorm(d_model)
        self.dropout = nn.Dropout(dropout)

    def forward(self, x, pos_emb, mask=None):
        x = x + 0.5 * self.ff1(self.norm_ff1(x))
        x = x + self.dropout(self.attn(self.norm_attn(x), pos_emb, mask))
        x = x + self.conv(self.norm_conv(x), mask)
        x = x + 0.5 * self.ff2(self.norm_ff2(x))
        return self.norm_out(x)


class ConformerEncoder(nn.Module):
    """Conformer-based speech encoder.
    
    Takes preprocessed mel spectrogram features and produces encoded representations.
    Input: audio_signal [B, 80, T], length [B]
    Output: encoded [B, d_model, T'], encoded_lengths [B]
    """

    def __init__(
        self,
        d_model=1024,
        n_heads=8,
        ff_dim=4096,
        conv_kernel_size=9,
        n_layers=24,
        n_mel=80,
        sub_channels=256,
        max_len=5000,
        dropout=0.1,
    ):
        super().__init__()
        self.d_model = d_model
        self.scale = math.sqrt(d_model)
        self.subsampling = ConvSubsampling(n_mel, d_model, sub_channels)
        self.pos_enc = RelativePositionalEncoding(d_model, max_len)
        self.layers = nn.ModuleList([
            ConformerBlock(d_model, n_heads, ff_dim, conv_kernel_size, dropout)
            for _ in range(n_layers)
        ])

    def forward(self, audio_signal, lengths):
        x, out_lengths = self.subsampling(audio_signal, lengths)
        x = x * self.scale
        T = x.size(1)
        pos_emb = self.pos_enc(T)

        mask = torch.arange(T, device=x.device).unsqueeze(0) < out_lengths.unsqueeze(1)

        for layer in self.layers:
            x = layer(x, pos_emb, mask)

        return x.transpose(1, 2), out_lengths           # [B, d_model, T'], [B]
FILE:model_meta/Shrutam-2/smear.py
import torch
import torch.nn as nn
import torch.nn.functional as F

class EncoderProjectorLinear(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.encoder_dim = config.encoder_dim
        self.llm_dim = config.llm_dim
        self.relu1 = nn.ReLU()
        self.linear1 = nn.Linear(self.encoder_dim, 2048)
        self.relu2 = nn.ReLU()
        self.linear2 = nn.Linear(2048, self.llm_dim)
    
    def forward(self, x):
        x = self.relu1(x)
        x = self.linear1(x)
        x = self.relu2(x)
        x = self.linear2(x)
        return x


class MoELayer_SMEAR(nn.Module):
    """
    Compact, vectorized SMEAR for EncoderProjectorLinear experts (2-layer MLP).
    Minimal Python loops: only used to stack expert params once per forward.
    """
    def __init__(self, experts: nn.ModuleList, input_dim: int, load_balancing_weight: float = 0.2):
        super().__init__()
        assert isinstance(experts, (list, nn.ModuleList)) and len(experts) > 0
        self.experts = experts
        self.num_experts = len(experts)
        self.input_dim = input_dim
        self.load_balancing_weight = load_balancing_weight

        # Router generates frame-wise logits
        self.router = nn.Linear(input_dim, self.num_experts)

    def forward(self, x, mask: torch.Tensor = None):
        """
        x: [B, L, D_in]
        mask (optional): [B, L] (1 for valid, 0 for pad)
        Returns:
            out: [B, L, out_dim]
            load_balance_loss: scalar tensor
            info: dict with utterance_probs and router_prob_mean
        """
        B, L, D_in = x.shape
        E = self.num_experts
        device = x.device
        dtype = x.dtype

        router_logits = self.router(x)                    # [B, L, E]
        router_probs = F.softmax(router_logits, dim=-1)   # [B, L, E]

        # optional mask: zero out padding positions
        if mask is not None:
            mask = mask.to(device=device, dtype=dtype)   # [B, L]
            router_probs = router_probs * mask.unsqueeze(-1)
            denom = mask.sum(dim=1, keepdim=True).clamp_min(1.0)  # [B,1]
        else:
            denom = float(L)

        router_prob_mean = router_probs.mean(dim=(0, 1))   # [E]
        ideal_prob = torch.ones_like(router_prob_mean, device=device) / E
        load_balance_loss = self.load_balancing_weight * F.mse_loss(router_prob_mean, ideal_prob)

        if mask is not None:
            utterance_probs = router_probs.sum(dim=1) / denom  # [B, E]
        else:
            utterance_probs = router_probs.mean(dim=1)         # [B, E]

        W1 = torch.stack([e.linear1.weight for e in self.experts], dim=0)  # [E, H, D_in]
        b1 = torch.stack([e.linear1.bias   for e in self.experts], dim=0)  # [E, H]
        W2 = torch.stack([e.linear2.weight for e in self.experts], dim=0)  # [E, out_dim, H]
        b2 = torch.stack([e.linear2.bias   for e in self.experts], dim=0)  # [E, out_dim]

        merged_W1 = torch.einsum('be,ehi->bhi', utterance_probs, W1)
        merged_b1 = torch.einsum('be,eh->bh', utterance_probs, b1)
        merged_W2 = torch.einsum('be,eoi->boi', utterance_probs, W2)
        merged_b2 = torch.einsum('be,eo->bo', utterance_probs, b2)

        merged_W1_T = merged_W1.transpose(1, 2)    # [B, D_in, H]
        hidden = torch.einsum('bld,bdh->blh', x, merged_W1_T) + merged_b1.unsqueeze(1)
        hidden = F.relu(hidden)

        merged_W2_T = merged_W2.transpose(1, 2)    # [B, H, out]
        out = torch.einsum('blh,bho->blo', hidden, merged_W2_T) + merged_b2.unsqueeze(1)

        info = {
            'utterance_probs': utterance_probs.detach(),
            'router_prob_mean': router_prob_mean.detach()
        }
        return out, load_balance_loss, info


FILE:model_meta/Shrutam-2/utils.py
import torch
import torch.nn as nn

def compute_accuracy(pad_outputs, pad_targets, ignore_label):
    """Calculate accuracy.

    Args:
        pad_outputs (LongTensor): Prediction tensors (B, Lmax).
        pad_targets (LongTensor): Target label tensors (B, Lmax).
        ignore_label (int): Ignore label id.

    Returns:
        float: Accuracy value (0.0 - 1.0).

    """
    mask = pad_targets != ignore_label
    numerator = torch.sum(
        pad_outputs.masked_select(mask) == pad_targets.masked_select(mask)
    )
    denominator = torch.sum(mask)
    return numerator.float() / denominator.float()

def print_model_size(model, config, rank: int = 0) -> None:
    """
    Log model name, the number of trainable parameters and initialization time.

    Args:
        model: The PyTorch model.
        model_name (str): Name of the model.
        init_time_start (float): Initialization start time.
        init_time_end (float): Initialization end time.
        rank (int, optional): Current process's rank. Defaults to 0.
    """
    if rank == 0:
        print((f"--> Model {config.model_name}"))
        total_params = sum(p.numel() for p in model.parameters() if p.requires_grad)
        print((f"--> {config.model_name} has {total_params / 1e6} Million params\n"))FILE:model_meta/Shrutam-2/llm/config.json
{
  "architectures": [
    "LlamaForCausalLM"
  ],
  "attention_bias": false,
  "attention_dropout": 0.0,
  "bos_token_id": 2,
  "dtype": "bfloat16",
  "eos_token_id": 128001,
  "head_dim": 64,
  "hidden_act": "silu",
  "hidden_size": 2048,
  "initializer_range": 0.01,
  "intermediate_size": 8192,
  "max_position_embeddings": 4096,
  "mlp_bias": false,
  "model_type": "llama",
  "num_attention_heads": 32,
  "num_hidden_layers": 16,
  "num_key_value_heads": 8,
  "pad_token_id": 0,
  "pretraining_tp": 1,
  "rms_norm_eps": 1e-05,
  "rope_scaling": null,
  "rope_theta": 10000,
  "tie_word_embeddings": true,
  "transformers_version": "4.57.3",
  "use_cache": false,
  "vocab_size": 128016
}
POINTER:model_meta/Shrutam-2/encoder.pt
version https://git-lfs.github.com/spec/v1
oid sha256:a7c7319afccba4bcc2a83cf1ab63235865f3669abd7337ba67cf2eb4c351d76d
size 2472053634
POINTER:model_meta/Shrutam-2/model.pt
version https://git-lfs.github.com/spec/v1
oid sha256:254134d3245195ebec22c15156e3f89383a5e64548c5e3dec9a9da1c989db3a5
size 5159634998
POINTER:model_meta/Shrutam-2/llm/model.safetensors
version https://git-lfs.github.com/spec/v1
oid sha256:d8752857591f40152710d37c57cce94ca809172acbe512567d63b18270f78c9f
size 2470662568
POINTER:model_meta/Shrutam-2/llm/tokenizer.json
version https://git-lfs.github.com/spec/v1
oid sha256:5d4c129eb61b2eb24838812aa48eeccc31a8af9fd0ec3b5d712a647ee36a5fc4
size 14113289
